circuit half_adder(x,y) { 
    let s = xor(x,y);
    let c = and(x,y);
    output(s);
    output(c);
}

circuit full_adder(x, y, cn) {
    let s = xor(x,y,cn);
    let cout = or(and(x,y), and(y, cn), and(x, cn));
    output(s);
    output(c);
}
